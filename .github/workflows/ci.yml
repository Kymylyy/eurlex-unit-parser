name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e .[dev]

      - name: Compile check
        run: |
          python -m py_compile parse_eu.py test_coverage.py run_batch.py download_eurlex.py

      - name: Lint
        run: |
          ruff check .

      - name: Typecheck
        run: |
          mypy src/eurlex_unit_parser

      - name: Run tests
        run: |
          pytest -q
